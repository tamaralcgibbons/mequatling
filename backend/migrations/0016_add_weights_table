"""Add weights table

Revision ID: xxxxxxxx
Revises: (previous_revision_id)
Create Date: 2025-08-18 (or current date)

"""
from alembic import op
import sqlalchemy as sa

# revision identifiers, used by Alembic.
revision = '0016_add_weights_table'
down_revision = '0015_add_notes_to_vaccine'
branch_labels = None
depends_on = None

def upgrade():
    op.create_table(
        'weights',
        sa.Column('id', sa.Integer(), primary_key=True),
        sa.Column('animal_id', sa.Integer(), sa.ForeignKey('animals.id'), nullable=False),
        sa.Column('weight', sa.Float(), nullable=False),
        sa.Column('date', sa.Date(), nullable=False),
    )

def downgrade():
    op.drop_table('weights')